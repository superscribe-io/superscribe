# superscribe_migrations isn't surfaced in the app, nor accessible from the API
table: superscribe_migrations

fields:
  - collection: superscribe_migrations
    field: version
  - collection: superscribe_migrations
    field: name
  - collection: superscribe_migrations
    field: timestamp
