import{_ as e,c as a,o as s,R as t}from"./chunks/framework.70e71619.js";const y=JSON.parse('{"title":"Relationships","description":"","frontmatter":{},"headers":[],"relativePath":"app/data-model/relationships.md","lastUpdated":1682552691000}'),n={name:"app/data-model/relationships.md"},o=t(`<h1 id="relationships" tabindex="-1">Relationships <a class="header-anchor" href="#relationships" aria-label="Permalink to &quot;Relationships&quot;">​</a></h1><blockquote><p>Relationships are a crucial part of any relational database. Directus supports all standard relationship types, as well as a few more of its own <em>compound</em> types, which are custom-tailored to make certain <em>common but complex</em> tasks a breeze.</p></blockquote><div class="tip custom-block"><p class="custom-block-title">Before You Begin</p><p>Regardless of the relationship you want to configure, we recommend you read every section of this document, in order, at least once. This is because you must understand how M2Os work in Directus to understand O2Ms, you must understand M2Os and O2Ms to understand M2Ms, etc.</p></div><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The Data Studio makes <em>the process</em> of configuring relational data models easier, faster, and more intuitive by offering no-code configuration. Directus <em>does not</em> enforce opinionated schemas, rule systems, or other arbitrary limitations to your data models. Therefore, aside from any technical limitations of your project&#39;s infrastructure or core requirements for any relational data model, <em>like having a primary key field for every collection or a data type assigned to every field,</em> you are free to build the data model as you want.</p><p>In this guide, we will go over the following topics:</p><ul><li>What kinds of relationships exist within Directus.</li><li>How to configure a desired relationship within the Data Studio.</li><li>How relationships are implemented in the data model and displayed in the Data Studio.</li><li>When it might be appropriate to use a given type of relationship.</li></ul><p>By the end, you&#39;ll understand everything needed to start building data models in Directus, even if relational data model concepts are a new concept to you.</p><h3 id="directus-vs-classic-data-model-terms" tabindex="-1">Directus vs Classic Data Model Terms <a class="header-anchor" href="#directus-vs-classic-data-model-terms" aria-label="Permalink to &quot;Directus vs Classic Data Model Terms&quot;">​</a></h3><p>When we use classic data model terms, <em>such as data table, column, row, etc...</em> this signals that the explanation is focused strictly on what happens in the database. When Directus terminology is used, <em>such as collection, field, item, etc...</em> this signals that the explanation includes Directus logic and functionality.</p><h2 id="many-to-one-m2o" tabindex="-1">Many-to-One (M2O) <a class="header-anchor" href="#many-to-one-m2o" aria-label="Permalink to &quot;Many-to-One (M2O)&quot;">​</a></h2><p>In an M2O relationship, multiple items from the parent collection are linked to one item in a related collection. For example, there are many cities in a country, but a city can only be in one country.</p><p>To create an M2O relationship, we add a foreign key field to the parent collection, which links items from the parent collection to items in the related collection. If we have two tables, <code>cities</code> and <code>countries</code>, we can create a <code>cities.country_id</code> foreign key field.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relations-20221026/m2o-20221026A.webp" alt="Many-to-One Relational Diagram"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">cities</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- country_id (a foreign key field, stores a key from countries.id)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">countries</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span></code></pre></div><p>Note the following things from the schema above:</p><ul><li>An M2O relationship requires just one column within the parent table.</li><li>When an M2O relational field is configured in Directus, an Item Page on the parent collection will enable access to the item from the related collection. So in our example above, an Item Page in <code>cities</code> will enable access to the related country from the <code>countries</code> table.</li></ul><p>However, in the Directus Data Studio, an M2O field does not automatically provide access to the parent collection&#39;s items within the related collection. In our example, this means that when you open an Item Page in <code>countries</code>, you will not see related cities.</p><p>This is where O2M fields come in to play.</p><div class="tip custom-block"><p class="custom-block-title">Configure an M2O</p><p>The easiest way to configure an M2O field is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select the M2O Interface from the template wizard.</p></div><h2 id="one-to-many-o2m" tabindex="-1">One-to-Many (O2M) <a class="header-anchor" href="#one-to-many-o2m" aria-label="Permalink to &quot;One-to-Many (O2M)&quot;">​</a></h2><p>Within a relational database, an O2M relationship is the exact same type of relationship as an M2O. Remember, at the end of the <a href="#many-to-one-m2o">M2O</a> section, we learned that configuring an M2O in Directus does not let us access related items within an Item Page on the related collection. In Directus, configuring an O2M creates an <a href="/getting-started/glossary.html#alias">Alias</a> field, which lets us access related items. To demonstrate this, let&#39;s continue with the <code>cities</code> and <code>countries</code> example relationship used in the M2O section.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relations-20221026/o2m-20221026A.webp" alt="One-to-Many Relational Diagram"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">countries</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- cities (the O2M alias field, does not exist in the data table. Allows access to all cities linked to a country.)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">cities</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- country_id (the M2O field)</span></span></code></pre></div><p>Note the following points from the schema above. When we create an O2M in Directus:</p><ul><li>Since the perspective is flipped, we now consider <code>countries</code> to be the parent collection.</li><li>It isn&#39;t always necessary to create an O2M. In some cases, you won&#39;t want or need to access items from both sides.</li><li>At first glance, this O2M alias field might make it <em>look and feel</em> like a new column was created, but the O2M field is purely <em>virtual</em>. It creates an Interface within the Data Studio to access items from an O2M perspective. In other words, the O2M alias field allows us to access any related items from <code>cities</code> within an Item Details Page in the <code>countries</code> collection.</li></ul><div class="tip custom-block"><p class="custom-block-title">Configure an O2M</p><p>The easiest way to configure an O2M is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select the O2M alias field type from the template wizard.</p></div><h2 id="one-to-one-o2o" tabindex="-1">One-to-One (O2O) <a class="header-anchor" href="#one-to-one-o2o" aria-label="Permalink to &quot;One-to-One (O2O)&quot;">​</a></h2><p>Directus does not include a dedicated One-to-One (O2O) relationship type or Interface. However, in the database, O2O is almost exactly the same as an M2O. The only difference is that an O2O enforces <em>cardinality</em>. In other words, one item from the parent collection can be linked with one item on the related collection, and vice-versa.</p><p>For example, each country has one capital city, and vice versa. This is an O2O. To demonstrate how it works, let&#39;s add this O2O to the <code>cities</code> and <code>countries</code> example relationship used in the previous sections.</p><p>The first strategy you may think of it to add a new <code>capital_city</code> field on the <code>countries</code> collection, storing the name of the capital city directly. But this would create <a href="/configuration/data-model.html#avoid-data-duplication">duplicate data</a>, because the same city would exist in both <code>countries.capital_city</code> as well as <code>cities.name</code>. But remember, we want to <em>avoid duplicate data!</em></p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relations-20221026/o2o-duplicate-20221026A.webp" alt="Duplicate Data from Capital Cities"></p><p>Instead, we want to use an O2O relationship. Let&#39;s try adding a <code>cities.capital_of</code> field.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relations-20221026/o2o-inefficient-20221026A.webp" alt="An inefficient One-to-One Relationship"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">cities</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- country_id</span></span>
<span class="line"><span style="color:#A6ACCD;">- capital_of (The O2O field. Actually an M2O, configured to store unique values, stores foreign key from countries.id)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">countries</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- cities</span></span></code></pre></div><p>The O2O relationship in the schema above works, and in some cases it may not matter which collection to configure the O2O onto. But in this case it is sub-optimal. Since <em>most cities</em> are not capital cities, the column will mostly contain <code>NULL</code> values. However, every single country has a capital city. So if we create the O2O on the <code>countries</code> collection, it will be much more efficient.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relations-20221026/o2o-20221026A.webp" alt="A One-to-One Relationship"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">countries</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- cities</span></span>
<span class="line"><span style="color:#A6ACCD;">- capital_city (The O2O field. Actually an M2O, configured to store unique values, stores foreign key from cities.id)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">cities</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- country_id</span></span></code></pre></div><p>Note the following points from the schema above. When we create an O2O in Directus:</p><ul><li>We can add the O2O field on either collection. However, in some cases it is more efficient to add it to a specific collection.</li><li>Since the O2O field is really just an M2O field behind the scenes, and since Directus doesn&#39;t automatically display M2O fields in the related collection, you may want to <a href="#one-to-many-o2m">configure an O2M field</a> so that you can access items from the related collection as well.</li></ul><div class="tip custom-block"><p class="custom-block-title">Configure an O2O</p><p>The easiest way to configure an O2O is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select the <strong>M2O</strong> field type from the template wizard. Then, configure the field&#39;s schema, toggling on <strong>Unique</strong> so that each value in the M2O field is unique, resulting in an O2O relationship.</p></div><h2 id="many-to-many-m2m" tabindex="-1">Many-to-Many (M2M) <a class="header-anchor" href="#many-to-many-m2m" aria-label="Permalink to &quot;Many-to-Many (M2M)&quot;">​</a></h2><p>The relationship types we have seen so far only required one foreign key column to link the parent collection and related collection. An M2M relationship is composed <em>of two foreign key columns</em> stored within an additional table, called a <em>junction table</em>, which stores each linked row between the parent table and related table.</p><p>Junction tables are required in M2M relationships because the number of relationships created can <em>(and often will!)</em> outnumber the number of rows in either data table. In other words, if you have <code>x</code> rows in the parent column and <code>y</code> rows in the related column, you need room to store up to <code>x * y</code> rows. Junction tables provide a place to store all the relationships between rows, no matter how many exist.</p><p>To demonstrate this, let&#39;s think about the relationship between recipes and ingredients: a <em>recipe</em> can have many <em>ingredients</em>, and <em>ingredients</em> can be in many <em>recipes</em>.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relationships-20220805/m2m-20220805A.webp" alt="Many-to-Many Relational Diagram"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">recipes</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- ingredients (An M2M alias field. Does not exist in the database, allows access to ingredients linked from recipe_ingredients)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">recipes_ingredients (junction collection)</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- recipe (stores foreign key from recipes.id)</span></span>
<span class="line"><span style="color:#A6ACCD;">- ingredient (stores foreign key from ingredients.id)</span></span>
<span class="line"><span style="color:#A6ACCD;">- quantity (A &quot;context&quot; field. Stores other data associated with the relationship. These are optional.)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">ingredients</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span></code></pre></div><p>Note the following points from the schema above. When we create an M2M in Directus:</p><ul><li>Our junction collection, <code>recipe_ingredients</code>, each row contains two foreign key columns. This is what creates the relationships between the two tables.</li><li>Assuming the M2M alias field is created within the <code>recipes</code> collection, Directus does not automatically add a field to display recipes within the <code>ingredients</code> collection. However, you can configure an alias field within <code>ingredients</code> if desired:</li></ul><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">ingredients</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- recipes (an O2M alias field, does not exist in the database, enables access to all the recipes related to an ingredient)</span></span></code></pre></div><ul><li>Notice that the junction collection also has a <code>quantity</code> field, which tracks how much of each ingredient is needed for the recipe. This is called a <em>contextual field</em>. The Data Studio provides full access to the junction collection, so you can add any number of contextual fields needed to the junction collection.</li><li>You can also have a self-referencing M2M relationship that connects items in the <em>same collection</em>. One example is &quot;Related Articles&quot;, where each article relates to many other articles.</li></ul><div class="tip custom-block"><p class="custom-block-title">Configure an M2M</p><p>The easiest way to configure an M2M is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select <strong>Many to Many</strong> from the template wizard.</p></div><h2 id="many-to-any-m2a" tabindex="-1">Many-to-Any (M2A) <a class="header-anchor" href="#many-to-any-m2a" aria-label="Permalink to &quot;Many-to-Any (M2A)&quot;">​</a></h2><p>Sometimes called a <em>matrix field</em> or <em>replicator</em>, an M2A relationship allows you to link items from the parent collection to any item in any collection in the database. When you configure an M2A in Directus, an M2A <a href="/getting-started/glossary.html#alias">Alias</a> field is created as well as a junction collection, like we saw on M2M relationships. The difference is that the junction collection on an M2A also has a field to store the <strong>collection key</strong><em>(the name of the collection)</em> for related collections.</p><p>One common example of when M2As are used is for <em>page builders</em>, which have a <code>pages</code> collection that combines multiple collections for each type of page section, such as <code>heading</code>, <code>text_bodies</code>, <code>image</code>, <code>video</code>, <em>etc</em>.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relationships-20220805/m2a-20220805A.webp" alt="Many-to-Any Relational Diagram"></p><p>Let&#39;s take a look at the schema:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">pages</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- name</span></span>
<span class="line"><span style="color:#A6ACCD;">- sections (An M2A alias field, does not exist in the database. Provides access to items from page_sections.)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">page_sections (junction collection)</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- pages_id (An M2O, stores foreign keys from pages.id)</span></span>
<span class="line"><span style="color:#A6ACCD;">- collection (An M2O, stores name of the related collection, for example headings, text_bodies, or images.)</span></span>
<span class="line"><span style="color:#A6ACCD;">- item (An M2O, stores foreign keys from headings.id, text.id, images.id, etc.)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">headings</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- title</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">text_bodies</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- text</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">images</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- file</span></span></code></pre></div><p>Note the following points from the schema above. When we create an M2A in Directus:</p><ul><li>Compared to the M2O and M2M relationships, there may be a lower likelihood that you will need to configure alias fields on related collections, such as <code>headings</code>, <code>text_bodies</code> and <code>images</code>, as these collections may not be as useful without the parent collection.</li><li>Each collection has a unique collection name, so this serves as an adequate foreign key in the <code>page_sections.collection</code> field.</li></ul><div class="tip custom-block"><p class="custom-block-title">Configure an M2A</p><p>The easiest way to configure an M2A is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select the <strong>Many to Any</strong> Interface from the template wizard.</p></div><h2 id="translations-o2m" tabindex="-1">Translations (O2M) <a class="header-anchor" href="#translations-o2m" aria-label="Permalink to &quot;Translations (O2M)&quot;">​</a></h2><p>Directus provides this special relational Interface designed specifically to handle translations. When you create a Translations O2M in the Data Studio, the following things happen within your data model. A Translations O2M alias field is created. A junction collection and a <code>languages</code> collection are created. All your translations are stored within context fields, configured by you, on the junction collection. Therefore, when you create a Translations O2M, you also create an M2M relationship behind the scenes. So remember, it is called the Translations O2M because we interact with the Translations O2M alias field. But behind the scenes, it is powered by an M2M.</p><p>To demonstrate, let&#39;s create a Translations O2M relationship for <code>articles</code>, a common content type that you may want to translate.</p><p><img src="https://cdn.directus.io/docs/v9/configuration/data-model/relationships/relationships-20220805/o2m-translations-20220805A.webp" alt="Translations O2M"></p><p>Let&#39;s take a look at the schema.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">articles</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- author (a field that is not translated)</span></span>
<span class="line"><span style="color:#A6ACCD;">- date_published (a field that is not translated)</span></span>
<span class="line"><span style="color:#A6ACCD;">- translations (A Translations O2M alias field, does not exist within the data table. Allows access to items from article_translations)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">article_translations</span></span>
<span class="line"><span style="color:#A6ACCD;">- id</span></span>
<span class="line"><span style="color:#A6ACCD;">- article_id (an M2O, stores foreign key article.id)</span></span>
<span class="line"><span style="color:#A6ACCD;">- language_id (an M2O, stores foreign key languages.language_code)</span></span>
<span class="line"><span style="color:#A6ACCD;">- title (A context field, created by you. Stores a translation of the Article Title)</span></span>
<span class="line"><span style="color:#A6ACCD;">- text (A context field, created by you. Stores a translation of the Article Text)</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">languages</span></span>
<span class="line"><span style="color:#A6ACCD;">- language_code (A primary key. A manually typed language code, e.g., &quot;en-US&quot;)</span></span>
<span class="line"><span style="color:#A6ACCD;">- name  (Stores the language name, e.g., &quot;English&quot;)</span></span></code></pre></div><p>Note the following points from the schema above. When we create a Translations O2M:</p><ul><li>As demonstrated by <code>article_translations.title</code> and <code>article_translations.text</code>, any <em>translated</em> fields should be added as context fields on the junction collection.</li><li>You are not bound to use this for translations. You can build your data model as desired. You could create individual fields for each translation, such as <code>title_english</code>, <code>title_german</code>, <code>title_french</code>, and so on. However, this is not easily extensible and it creates a sub-optimal experience to have every single translation of every field on the item details page. The Translations O2M alias field is designed specifically to make the translation process easier.</li><li>There may come a time when you want to make a pre-existing parent field translatable. To do this, you can <a href="/configuration/data-model/fields/#duplicate-a-field">duplicate a field</a>, move it to the translation collection, and then delete the parent field. However, be aware that duplicating a field does <em>not</em> duplicate any existing field values.</li></ul><div class="tip custom-block"><p class="custom-block-title">Configure a Translations O2M</p><p>The easiest way to configure a Translations relationship is to follow the guide on how to <a href="/configuration/data-model/fields/#create-a-field-standard">create a field (standard)</a> and select the <strong>Translations O2M</strong> Interface from the template wizard.</p></div>`,87),i=[o];function l(c,r,p,d,h,u){return s(),a("div",null,i)}const f=e(n,[["render",l]]);export{y as __pageData,f as default};
